import 'package:flutter/cupertino.dart';
import 'package:movie_flutter/utils/dimensions.dart';

class GetAPI extends StatelessWidget {
  const GetAPI({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text(
      "he", style: TextStyle(
      fontSize: Dimentions.with10,
    ),
    );
  }
}
